/**
 * Created by gustavoquesada on 10/25/16.
 */
export class Client {

    constructor (idClient, name, secret) {
        this.idClient = idClient;
        this.name = name;
        this.secret = secret;
    }
}